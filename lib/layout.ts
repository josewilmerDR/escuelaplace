/**
 * Shared `sizes` for page-width cover images. The content column is max-w-4xl
 * (896px) minus padding, so on wide viewports the image renders at ~848px.
 */
export const PAGE_COVER_SIZES = "(min-width: 896px) 848px, 100vw";
